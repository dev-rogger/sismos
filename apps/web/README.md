# web

Next.js 16 App Router frontend for `sismos` — PWA (Serwist) + Tailwind v4.

## Desarrollo

```bash
pnpm --filter web dev
```

Ver el README raíz del monorepo y `docs/superpowers/specs/` para contexto del proyecto.
